import { StyleSheet } from "react-native";

const cameraZoomItemStyleSheet = StyleSheet.create({
    viewContainer: {
        width: 5,
        alignItems: "center",
        borderRadius: 5
    },
});

export default cameraZoomItemStyleSheet;

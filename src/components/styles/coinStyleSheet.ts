import { StyleSheet } from "react-native";

const coinStyleSheet = StyleSheet.create({
    container: {
        justifyContent: "center",
        alignItems: "center",
    },
});

export default coinStyleSheet;

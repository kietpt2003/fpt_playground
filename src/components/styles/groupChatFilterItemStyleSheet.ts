import { StyleSheet } from "react-native";

const groupChatFilterItemStyleSheet = StyleSheet.create({
    viewContainer: {
        borderWidth: 1,
        borderRadius: 12,
    },
    txt: {
        fontFamily: "Roboto"
    }
});

export default groupChatFilterItemStyleSheet;

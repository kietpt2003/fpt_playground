import { StyleSheet } from "react-native";

const groupChatFiltersStyleSheet = StyleSheet.create({
    flatListStyle: {
        flexGrow: 0
    },
    flatListContentContainerStyle: {
        marginBottom: 20
    }
});

export default groupChatFiltersStyleSheet;

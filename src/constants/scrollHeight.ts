import { ScreenHeight } from "@rneui/base";

export const homeHeaderImageHeight = ScreenHeight / 3.5;

// export const featureComponentHeight = (ScreenHeight / 9) * 2 + 55;
export const featureComponentHeight = (ScreenHeight / 9) * 2 + 10;

// export const dailyCheckPointHeight = ScreenHeight / 4.5 + 70;
export const dailyCheckPointHeight = ScreenHeight / 4.5 + 20;

export const coveredNumber = ScreenHeight <= 920 ? 30 : 10;

import { ScreenWidth } from "@rneui/base";

export const sliderWidth = ScreenWidth - 50; // Chiều dài của Slider (cố định hoặc tính toán từ style)
export const sliderMin = 0;
export const sliderMax = 10;